# ASDM Default Rules - 自动构建策略

## 项目技术栈

- **前端**: Vue 3 + TypeScript + Vite（`web/qa-web`）
- **后端**: Java Spring Boot + Maven（`server/qa-service-user`、`server/qa-service-question`）
- **根项目**: Node.js + concurrently

## 构建命令

- **前端构建**: `cd web/qa-web && npm run build`（执行 `vue-tsc -b && vite build`）
- **根项目构建**: `npm run build`（构建前端）
- **后端用户服务启动**: `cd server/qa-service-user && ./mvnw spring-boot:run`
- **后端问答服务启动**: `cd server/qa-service-question && ./mvnw spring-boot:run`

## ASDM CLI 信息

- **可执行路径**: `/Users/botao/.nvm/versions/node/v24.13.0/bin/asdm`
- **安装方式**: npm 全局安装（`@leansoftx/asdm-bootstrapper-cli`）
- **CLI 版本**: 1.2.29916

## 自动构建策略（强制）

每次修改代码后，AI 必须执行以下操作：

1. **前端代码修改**：执行 `cd web/qa-web && npm run build`
2. **后端代码修改**：执行对应服务的 Maven 编译命令
3. **根项目修改**：执行 `npm run build`

若构建失败：
- 自动分析编译错误信息
- 修复所有编译错误
- 重新执行构建命令
- 重复此过程直至构建成功

不得跳过构建验证步骤。
