# Test Plan 文档规范

## 概述

此规范定义功能测试计划（Test Plan）文档的结构模板，供 `qahc-harness-testplan` action 基于 PRD 文档生成功能测试计划时参照。本规范**聚焦于功能测试**——验证系统行为是否符合 PRD 定义的功能需求。文档以测试套件（Phase）组织测试用例（Task），每个用例包含明确的测试步骤。

## 文档命名

```text
FT-{id}-{name}-TestPlan.md
```

存放路径：`.asdm/workspace/qahc-harness/feat/FT-{id}-{name}/`

## Test Plan 文档标准结构

```markdown
# FT-{id} {特性名称} — 测试计划

> **🔗 前置依赖**：本文档基于 [FT-{id}-{name}-PRD.md](./FT-{id}-{name}-PRD.md)（产品需求文档）生成。

> 最后更新：{YYYY-MM-DD}

---

## 测试概述

<!-- 2~3 句话概述测试范围、目标和重点区域 -->

### 测试环境

| 环境项 | 说明 |
| ------ | ------ |
| 测试分支 | {分支名} |
| 数据库 | {数据库信息} |
| 依赖服务 | {列出依赖的外部服务} |

### 前置条件

<!-- 列表：测试执行前需满足的条件 -->

---

## 测试进度概要

| 测试套件 | 测试用例 | 状态 | 覆盖功能 |
| :----: | ------ | :----: | ------ |
| 1 | {用例名} | ⏳ | {功能点} |

---

## 测试套件 {N}: {套件名称}

### 测试目标

<!-- 1~2 句话描述该套件的测试目标 -->

### 测试用例 {N}.{T}: {用例名称}

**测试类型**：功能测试 / 集成测试 / 回归测试 / 边界测试 / 性能测试

**覆盖功能**：{对应的功能点或使用场景}

**前置条件**：
- {条件1}
- {条件2}

**测试数据**：

| 数据项 | 值 | 说明 |
| ------ | ------ | ------ |
| {字段} | {值} | {说明} |

#### 测试步骤

| 步骤 | 操作 | 预期结果 |
| :----: | ------ | ------ |
| 1 | {操作描述} | {预期结果} |
| 2 | {操作描述} | {预期结果} |

---

<!-- 按需添加更多测试套件和测试用例 -->

## 测试总结

> 以下在测试执行过程中动态更新

| 测试用例 | 状态 | 执行日期 | 结论 | 备注 |
| :------: | :----: | ------ | ------ | ------ |
| 1.1 | ❌ 未执行 | - | - | - |

### 通过率

| 指标 | 数值 |
| ------ | :----: |
| 总用例数 | N |
| 已执行 | 0 |
| 通过 | 0 |
| 失败 | 0 |
| 阻塞 | 0 |
| 通过率 | 0% |
```

### 测试套件命名

```markdown
## 测试套件 {N}: {套件名称}
```

**示例**：
- `测试套件 1: 用户注册登录`
- `测试套件 2: 医疗问题发布与回答`
- `测试套件 3: 边界异常处理`

## 测试套件（Phase）划分原则

| 原则 | 说明 |
| ------ | ------ |
| 场景覆盖 | 每个套件对应 PRD 中的一个使用场景或功能模块 |
| 独立可测 | 套件之间保持独立，减少交叉依赖 |
| 优先级排序 | 核心流程 → 辅助功能 → 边界异常 |

## 测试用例（Task）编写规范

### 用例编号

格式：`{Suite}.{Case}` 如 `1.1`、`2.3`

### 测试类型

| 类型 | 适用场景 | 典型特征 |
|------|----------|----------|
| 功能测试 | 验证功能正确性 | 给定输入 → 验证输出是否符合预期 |
| 边界测试 | 验证边界条件 | 极值、空值、超长输入 |
| 异常测试 | 验证异常处理 | 无效输入、缺失参数、未授权访问 |

### 测试步骤格式

每个测试步骤使用统一格式：

| 步骤 | 操作 | 预期结果 |
| :----: | ------ | ------ |

- **步骤编号**：从 1 开始递增
- **操作**：具体的执行动作，可使用 curl 命令、UI 操作描述、API 调用等
- **预期结果**：明确的可验证结果，包括 HTTP 状态码、响应字段、数据库状态等

### 测试数据规范

- 测试数据应与预期结果对应
- 区分正常数据和异常数据
- 标注数据的特殊含义（如边界值、无效值）

## 测试执行与统计

### 状态标记

| 状态 | 图标 | 含义 |
|------|:----:|------|
| 未执行 | ❌ | 尚未执行 |
| 执行中 | ⏳ | 正在执行 |
| 通过 | ✅ | 所有步骤通过 |
| 失败 | ❎ | 步骤未通过 |
| 阻塞 | 🚫 | 因环境/依赖问题无法执行 |
